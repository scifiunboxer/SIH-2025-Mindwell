# Dependencies (these folders contain thousands of files and are not needed)
/node_modules
/client/node_modules
/server/node_modules

# Secrets (VERY, VERY IMPORTANT - never upload this file)
/server/.env

# Production build folder (generated automatically)
/client/build

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc editor/OS files
.DS_Store